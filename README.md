# Weather App
A simple CLI weather app using the OpenWeatherMap API.

## Setup
1. Add your API key to a .env file: \`API_KEY=your_key\`
2. Run: \`pip install requests python-dotenv\`
3. Run: \`python weather.py\`
